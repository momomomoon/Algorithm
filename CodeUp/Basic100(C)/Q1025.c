#include <stdio.h>
int main() {
    int a, b, c, d, f;
    scanf("%1d %1d %1d %1d %1d", &a, &b, &c, &d, &f );
    printf("[%d]\n", a * 10000);
    printf("[%d]\n", b * 1000);
    printf("[%d]\n", c * 100);
    printf("[%d]\n", d * 10);
    printf("[%d]\n", f);
    return 0;
} 